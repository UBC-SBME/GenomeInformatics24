# GenomeInformatics24

Note that you should only look at the parts of this repo linked to via canvas as other parts will be out of date. The version that is available when the assignmnet is posted is the version to use and if you clone the repo before then, you may have an out of date version and will lose marks.
